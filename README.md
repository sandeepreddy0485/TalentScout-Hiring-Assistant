# TalentScout Hiring Assistant\n\nAn AI/ML-inspired recruitment screening chatbot built with **Python** and **Streamlit**. It collects candidate info, generates tech-specific interview questions, and saves the candidate profile to JSON.\n\n## ✅ Project overview\n- Chatbot name: **TalentScout Hiring Assistant**\n- Collects candidate data step-by-step: name, email, phone, experience, position, location, tech stack\n- Detects tech stack items and generates 3–5 technical questions per item\n- Maintains conversation context, handles unclear user input, and supports exit keywords\n- Stores candidate profiles in `data/candidates.json`\n- Uses clean modules: `app.py`, `utils.py`, `prompt_templates.py`\n\n## ⚙️ Installation\n1. Clone or create folder:\n `\n   cd c:\\Users\\yaram\\Desktop\\talentscout-hiring-assistant\n   `\n2. Setup virtual env (recommended):\n `\n   python -m venv .venv\n   .venv\\Scripts\\activate\n   `\n3. Install requirements:\n `\n   pip install -r requirements.txt\n   `\n4. Create `.env` (optional):\n `\n   OPENAI_API_KEY=your_api_key\n   `\n NOTE: This sample doesn’t require OpenAI key to run, but could be extended if needed.\n\n## ▶️ Run\n`\nstreamlit run app.py\n`\n\n## 🧠 Usage\n1. Enter full name, then proceed through each requested field.\n2. Tech stack input example: `Python, Django, React, SQL`.\n3. Bot generates a technical question set and shows a thank you message.\n4. Check `data/candidates.json` for stored record.\n\n## 🛠️ Prompt design\n`prompt_templates.py` defines:

- `GREETING_TEXT` for welcome flow
- `ABOUT_TEXT` for UI context
- `CONFUSED_TEXT` for fallback invalid entry
- `EXIT_TEXT` reconstruction when user decides to end
- `THANK_YOU_TEXT` for completion messaging\n\n`utils.py` defines:
- Input validation for email, phone, experience
- Exit command detection
- Tech stack normalization and question lookup
- JSON persistence for candidate profiles\n\n## 🎯 Conversation flow logic (`app.py`):\n1. Start with greeting + instructions.\n2. Step-by-step fields guided by `FIELDS`.\n3. Validate input; fallback on invalid.\n4. On tech stack finalization, parse and produce questions.\n5. Store complete candidate object in JSON.\n6. Thank candidate and end session.\n\n## 🧪 Example conversation output\n> TalentScout: Welcome ...\n> You: John Doe\n> TalentScout: Please enter your email...\n> You: john@example.com\n> ...\n> TalentScout: Thanks! Here are your generated technical screening questions:\n> TalentScout: **python**\n> TalentScout: • Explain list comprehension ...\n> TalentScout: **django**\n> ...\n> TalentScout: Thank you for completing the screening steps. ...\n\n## 💡 Optional enhancements (bonus)\n- Add sentiment engine (TextBlob, NLTK, etc.)\n- Add multi-language support for prompts and validation\n- Add OpenAI GPT-powered dialogue and question generation\n- Add persistent user session and chatbot `history.json`\n\n## 🧾 Challenges and solutions\n- Challenge: maintain conversational state in Streamlit.\n - Solution: use `st.session_state` status machine (`step/ended/messages`).\n- Challenge: technology detection across synonyms.\n - Solution: normalized mapping and fallback question templates.\n- Challenge: user might send non-valid structured data.\n - Solution: validation for email/phone/experience and fallback text.\n
